<html>
    <head></head>
    <body>
        <h1>List Club</h1>
        <img src="{{ asset('webApp.png') }}">
        <table border="2">
            <tr><th>Name</th><th>Address</th><th>nbr_User</th></tr>
            {% for i in list %}
                <tr>
                    <td>{{ i.name }}</td>
                    <td>{{ i.address }}</td>
                    <td>{{ i.nbr_User }}</td>
                </tr>
            {%  endfor %}
        </table>
    </body>
</html>
