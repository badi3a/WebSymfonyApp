<html>
<head></head>
<body>
    <h1>Liste des classes</h1>
    <table border="2">
        <tr><th>id</th><th>Name</th><th>Action</th></tr>
        <tr {% for c in list %}>

            <td>{{ c.id }}</td>
            <td>{{ c.name }}</td>
            <td>
                <a href="{{ path("deleteClassPage",{'id':c.id}) }}">
                  <button>Supprimer</button>
                </a>
                <a href="{{ path("UpdateClassPage",{'id':c.id}) }}">
                    <button>Modifier</button>
                </a>
            </td>

        </tr {% endfor %}>
    </table>
</body>
</html>
